services:
  - type: web
    name: car-price-prediction
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT -w 2 'app:create_app()'
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
